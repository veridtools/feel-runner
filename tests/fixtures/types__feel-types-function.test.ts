// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { n } from '../tck/utils.js';

describe('dmn-fixtures / feel-types / feel-types/function', () => {
  describe('feel-conditional-short-circuit', () => {
    const EXPR =
      '{\n  true_branch_no_error: if true then 1 else 1 / 0,\n  false_branch_no_error: if false then 1 / 0 else 2,\n  guarded_access: if {a: 1}.b != null then {a: 1}.b else 0,\n  guard_non_empty: if count([1, 2]) > 0 then [1, 2][1] else 0,\n  guard_empty: if count([]) > 0 then [][1] else -1,\n  nested_if: if true then (if false then 1/0 else 5) else 1/0,\n  short_circuit_and: true or (1 / 0 = 1),\n  short_circuit_or: false and (1 / 0 = 1),\n  ternary_like: if 1 > 0 then "positive" else "non-positive",\n  null_guard: if null != null then 1 else 0\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        true_branch_no_error: 1,
        false_branch_no_error: 2,
        guarded_access: 0,
        guard_non_empty: 1,
        guard_empty: -1,
        nested_if: 5,
        short_circuit_and: true,
        short_circuit_or: false,
        ternary_like: 'positive',
        null_guard: 0,
      });
    });
  });
  describe('feel-function-self-recursive', () => {
    const EXPR =
      '{\n  fn: function(n) if n <= 1 then 1 else n * fn(n - 1),\n  result: { fact0: fn(0), fact1: fn(1), fact5: fn(5), fact6: fn(6) }\n}.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({ fact0: 1, fact1: 1, fact5: 120, fact6: 720 });
    });
  });
  describe('feel-instance-of-any', () => {
    const EXPR =
      '{\n  number_any: 1 instance of Any,\n  string_any: "hello" instance of Any,\n  null_any: null instance of Any,\n  list_any: [1,2] instance of Any,\n  context_any: {a:1} instance of Any,\n  bool_any: true instance of Any,\n  date_any: @"2024-01-15" instance of Any,\n  fn_any: (function(x) x) instance of Any\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        number_any: true,
        string_any: true,
        null_any: false,
        list_any: true,
        context_any: true,
        bool_any: true,
        date_any: true,
        fn_any: true,
      });
    });
  });
  describe('feel-instance-of-context-type', () => {
    const EXPR =
      '{\n  context_basic: {a: 1} instance of context,\n  context_not_number: {a: 1} instance of number,\n  number_not_context: 1 instance of context,\n  empty_context: {} instance of context,\n  nested_context: {a: {b: 1}} instance of context,\n  list_not_context: [1,2] instance of context\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        context_basic: true,
        context_not_number: false,
        number_not_context: false,
        empty_context: true,
        nested_context: true,
        list_not_context: false,
      });
    });
  });
  describe('feel-instance-of-function-type', () => {
    const EXPR =
      '{\n  fn_typed: (function(x) x + 1) instance of function<number>->number,\n  fn_wrong_type: 42 instance of function<number>->number,\n  string_not_fn: "hello" instance of function<number>->number,\n  two_param_fn: (function(a,b) a + b) instance of function<number,number>->number,\n  null_not_fn: null instance of function<number>->number\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        fn_typed: true,
        fn_wrong_type: false,
        string_not_fn: false,
        two_param_fn: true,
        null_not_fn: false,
      });
    });
  });
  describe('feel-instance-of-list-type', () => {
    const EXPR =
      '{\n  list_number: [1,2,3] instance of list<number>,\n  list_string: ["a","b"] instance of list<string>,\n  list_not_number: ["a","b"] instance of list<number>,\n  list_empty: [] instance of list<number>,\n  list_mixed_fails: [1,"a"] instance of list<number>,\n  number_not_list: 1 instance of list<number>,\n  nested_list: [[1,2],[3,4]] instance of list<list<number>>\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        list_number: true,
        list_string: true,
        list_not_number: false,
        list_empty: true,
        list_mixed_fails: false,
        number_not_list: false,
        nested_list: true,
      });
    });
  });
  describe('feel-instance-of-null', () => {
    const EXPR =
      '{\n  null_is_null_type: null instance of Null,\n  null_not_number: null instance of number,\n  null_not_string: null instance of string,\n  null_not_bool: null instance of boolean,\n  number_not_null_type: 1 instance of Null,\n  string_not_null_type: "a" instance of Null,\n  bool_not_null_type: false instance of Null\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        null_is_null_type: true,
        null_not_number: false,
        null_not_string: false,
        null_not_bool: false,
        number_not_null_type: false,
        string_not_null_type: false,
        bool_not_null_type: false,
      });
    });
  });
  describe('ft-function-definition', () => {
    const EXPR = '(function(x) x * 2)(5)';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(10);
    });
  });
  describe('ft-function-higher-order', () => {
    const EXPR =
      '{ apply: function(f, x) f(x), double: function(x) x * 2, result: apply(double, 5) }.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(10);
    });
  });
  describe('ft-function-instance-of', () => {
    const EXPR = '(function(x) x * 2) instance of function<number>->number';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(true);
    });
  });
  describe('ft-function-multi-param', () => {
    const EXPR = '{ add3: function(a, b, c) a + b + c, result: add3(1, 2, 3) }.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(6);
    });
  });
  describe('ft-function-named-param', () => {
    const EXPR = '{ f: function(x, y) x + y, result: f(x: 3, y: 4) }.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(7);
    });
  });
  describe('ft-function-recursive', () => {
    const EXPR = '{ square: function(x) x * x, result: square(7) }.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(49);
    });
  });
  describe('ft-function-type-check', () => {
    const EXPR = '(function(x) x + 1)(10) instance of number';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual(true);
    });
  });
  describe('ft-function-with-input', () => {
    const EXPR = '{ f: function(x) x * base, result: f(3) }.result';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { base: 10 }).value)).toEqual(30);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { base: 5 }).value)).toEqual(15);
    });
  });
});
