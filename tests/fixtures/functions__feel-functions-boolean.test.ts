// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { n } from '../tck/utils.js';

describe('dmn-fixtures / feel-functions / feel-functions/boolean', () => {
  describe('fn-even', () => {
    const EXPR = 'even(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 2 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: 3 }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: -4 }).value)).toEqual(true);
    });
  });
  describe('fn-instance-of', () => {
    const EXPR = 'val instance of number';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { val: 42 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { val: 'hello' }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { val: null }).value)).toEqual(false);
    });
  });
  describe('fn-is-defined-context-key', () => {
    const EXPR =
      '{\n  existing_key: is defined({a: 1, b: 2}.a),\n  missing_key: is defined({a: 1, b: 2}.c),\n  empty_ctx_key: is defined({}.a),\n  null_value_key: is defined({a: null}.a),\n  nested_existing: is defined({a: {b: 1}}.a.b),\n  nested_missing: is defined({a: {b: 1}}.a.c),\n  null_is_defined: is defined(null),\n  number_is_defined: is defined(42),\n  list_is_defined: is defined([1,2,3])\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        existing_key: true,
        missing_key: false,
        empty_ctx_key: false,
        null_value_key: false,
        nested_existing: true,
        nested_missing: false,
        null_is_defined: false,
        number_is_defined: true,
        list_is_defined: true,
      });
    });
  });
  describe('fn-is-defined', () => {
    const EXPR = 'is defined(val)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { val: 42 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { val: 'hello' }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { val: null }).value)).toEqual(false);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { val: false }).value)).toEqual(true);
    });
    it('case 5', () => {
      expect(n(evaluate(EXPR, { val: [] }).value)).toEqual(true);
    });
  });
  describe('fn-not-is-defined-edge', () => {
    const EXPR =
      '{\n  not_null: not(null),\n  not_true: not(true),\n  not_false: not(false),\n  is_defined_number: is defined(42),\n  is_defined_string: is defined("hello"),\n  is_defined_null: is defined(null),\n  is_defined_input: is defined(X),\n  odd_zero: odd(0),\n  even_zero: even(0),\n  odd_neg: odd(-3),\n  even_neg: even(-4),\n  odd_float: odd(1.5),\n  even_float: even(2.0)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { X: 42 }).value)).toEqual({
        not_null: null,
        not_true: false,
        not_false: true,
        is_defined_number: true,
        is_defined_string: true,
        is_defined_null: false,
        is_defined_input: true,
        odd_zero: false,
        even_zero: true,
        odd_neg: true,
        even_neg: true,
        odd_float: null,
        even_float: true,
      });
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { X: null }).value)).toEqual({
        not_null: null,
        not_true: false,
        not_false: true,
        is_defined_number: true,
        is_defined_string: true,
        is_defined_null: false,
        is_defined_input: false,
        odd_zero: false,
        even_zero: true,
        odd_neg: true,
        even_neg: true,
        odd_float: null,
        even_float: true,
      });
    });
  });
  describe('fn-not', () => {
    const EXPR = 'not(a)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: true }).value)).toEqual(false);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: false }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: null }).value)).toEqual(null);
    });
  });
  describe('fn-odd', () => {
    const EXPR = 'odd(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: 2 }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: -3 }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(false);
    });
  });
});
