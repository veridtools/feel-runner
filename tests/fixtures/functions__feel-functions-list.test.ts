// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { n } from '../tck/utils.js';

describe('dmn-fixtures / feel-functions / feel-functions/list', () => {
  describe('fn-all', () => {
    const EXPR = 'all(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [true, true] }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [true, false] }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [true, null] }).value)).toEqual(null);
    });
  });
  describe('fn-any', () => {
    const EXPR = 'any(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [false, true] }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [false, false] }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(false);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [null, true] }).value)).toEqual(true);
    });
  });
  describe('fn-append', () => {
    const EXPR = 'append(lst, item)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2], item: 3 }).value)).toEqual([1, 2, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [], item: 'x' }).value)).toEqual(['x']);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [1], item: null }).value)).toEqual([1, null]);
    });
  });
  describe('fn-concatenate', () => {
    const EXPR = 'concatenate(a, b)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: [1, 2], b: [3, 4] }).value)).toEqual([1, 2, 3, 4]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: [], b: [1] }).value)).toEqual([1]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: ['x'], b: ['y', 'z'] }).value)).toEqual(['x', 'y', 'z']);
    });
  });
  describe('fn-count', () => {
    const EXPR = 'count(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual(3);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(0);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: ['a', 'b', 'c', 'd'] }).value)).toEqual(4);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: null }).value)).toEqual(null);
    });
  });
  describe('fn-distinct-union-edge', () => {
    const EXPR =
      '{\n  distinct_dups: distinct values([1, 2, 1, 3, 2]),\n  distinct_nulls: distinct values([null, 1, null, 2]),\n  distinct_empty: distinct values([]),\n  union_two: union([1, 2], [2, 3, 4]),\n  union_overlap: union([1, 2, 3], [2, 3, 4, 5]),\n  union_empty: union([], [1, 2]),\n  union_both_empty: union([], [])\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        distinct_dups: [1, 2, 3],
        distinct_nulls: [null, 1, 2],
        distinct_empty: [],
        union_two: [1, 2, 3, 4],
        union_overlap: [1, 2, 3, 4, 5],
        union_empty: [1, 2],
        union_both_empty: [],
      });
    });
  });
  describe('fn-distinct-values', () => {
    const EXPR = 'distinct values(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 1, 3] }).value)).toEqual([1, 2, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: ['a', 'b', 'a'] }).value)).toEqual(['a', 'b']);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
  });
  describe('fn-duplicate-values', () => {
    const EXPR = 'duplicate values(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 1, 3, 2] }).value)).toEqual([1, 2]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual([]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
  });
  describe('fn-flatten-deep', () => {
    const EXPR =
      '{\n  already_flat: flatten([1, 2, 3]),\n  one_level: flatten([[1, 2], [3, 4]]),\n  two_levels: flatten([[1, [2]], [3]]),\n  three_levels: flatten([[[1]], [2, [3]]]),\n  empty: flatten([]),\n  with_null: flatten([[1, null], [2]])\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        already_flat: [1, 2, 3],
        one_level: [1, 2, 3, 4],
        two_levels: [1, 2, 3],
        three_levels: [1, 2, 3],
        empty: [],
        with_null: [1, null, 2],
      });
    });
  });
  describe('fn-flatten', () => {
    const EXPR = 'flatten(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, [2, 3], [4, [5]]] }).value)).toEqual([1, 2, 3, 4, 5]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual([1, 2, 3]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
  });
  describe('fn-index-of', () => {
    const EXPR = 'index of(lst, match)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3, 2], match: 2 }).value)).toEqual([2, 4]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], match: 4 }).value)).toEqual([]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: ['a', 'b', 'a'], match: 'a' }).value)).toEqual([1, 3]);
    });
  });
  describe('fn-insert-before', () => {
    const EXPR = 'insert before(lst, pos, item)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], pos: 2, item: 10 }).value)).toEqual([1, 10, 2, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2], pos: 1, item: 0 }).value)).toEqual([0, 1, 2]);
    });
  });
  describe('fn-list-contains', () => {
    const EXPR = 'list contains(lst, elem)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], elem: 2 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], elem: 4 }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: ['a', 'b'], elem: 'a' }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [], elem: 1 }).value)).toEqual(false);
    });
  });
  describe('fn-list-replace', () => {
    const EXPR =
      '{\n  replace_first: list replace([1, 2, 3], 1, 99),\n  replace_middle: list replace([1, 2, 3], 2, 99),\n  replace_last: list replace([1, 2, 3], 3, 99),\n  replace_match_fn: list replace([1, 2, 3, 2, 1], function(item, newItem) item = newItem, 99),\n  replace_match_even: list replace([1, 2, 3, 4, 5], function(item, newItem) modulo(item, 2) = 0, 0),\n  replace_string: list replace(["a", "b", "c"], 2, "X"),\n  replace_named_position: list replace(list: [10, 20, 30], position: 1, newItem: 0),\n  replace_out_of_bounds: list replace([1, 2, 3], 10, 99),\n  replace_single: list replace([42], 1, 99)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        replace_first: [99, 2, 3],
        replace_middle: [1, 99, 3],
        replace_last: [1, 2, 99],
        replace_match_fn: [1, 2, 3, 2, 1],
        replace_match_even: [1, 0, 3, 0, 5],
        replace_string: ['a', 'X', 'c'],
        replace_named_position: [0, 20, 30],
        replace_out_of_bounds: null,
        replace_single: [99],
      });
    });
  });
  describe('fn-list-vacuous-truth', () => {
    const EXPR =
      '{\n  all_empty: all([]),\n  any_empty: any([]),\n  all_true: all([true, true, true]),\n  all_with_false: all([true, false, true]),\n  any_with_true: any([false, true, false]),\n  any_all_false: any([false, false]),\n  all_null: all([null, null]),\n  any_null: any([null, null]),\n  all_mixed_null: all([true, null]),\n  any_mixed_null: any([false, null])\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        all_empty: true,
        any_empty: false,
        all_true: true,
        all_with_false: false,
        any_with_true: true,
        any_all_false: false,
        all_null: null,
        any_null: null,
        all_mixed_null: null,
        any_mixed_null: null,
      });
    });
  });
  describe('fn-max', () => {
    const EXPR = 'max(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [3, 1, 2] }).value)).toEqual(3);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [10] }).value)).toEqual(10);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [-5, -1, -3] }).value)).toEqual(-1);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(null);
    });
  });
  describe('fn-mean', () => {
    const EXPR = 'mean(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual(2);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 3] }).value)).toEqual(2);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(null);
    });
  });
  describe('fn-median', () => {
    const EXPR = 'median(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual(2);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3, 4] }).value)).toEqual(2.5);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [3, 1, 2] }).value)).toEqual(2);
    });
  });
  describe('fn-min', () => {
    const EXPR = 'min(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [3, 1, 2] }).value)).toEqual(1);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [10] }).value)).toEqual(10);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [-5, -1, -3] }).value)).toEqual(-5);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(null);
    });
  });
  describe('fn-mode', () => {
    const EXPR = 'mode(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 2, 3] }).value)).toEqual([2]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 1, 2, 2, 3] }).value)).toEqual([1, 2]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
  });
  describe('fn-named-params-list', () => {
    const EXPR =
      '{\n  sublist_named: sublist(list: [1, 2, 3, 4, 5], position: 2),\n  sublist_len_named: sublist(list: [1, 2, 3, 4, 5], position: 2, length: 3),\n  append_named: append(list: [1, 2, 3], item: 4),\n  insert_named: insert before(list: [1, 2, 3], position: 2, newItem: 99),\n  remove_named: remove(list: [1, 2, 3, 4], position: 2),\n  index_of_named: index of(list: [1, 2, 3, 2, 1], match: 2),\n  flatten_named: flatten(list: [[1, 2], [3, [4, 5]]]),\n  count_named: count(list: [1, 2, 3]),\n  min_named: min(list: [3, 1, 4, 1, 5]),\n  max_named: max(list: [3, 1, 4, 1, 5]),\n  sum_named: sum(list: [1, 2, 3, 4, 5]),\n  mean_named: mean(list: [2, 4, 6]),\n  all_named: all(list: [true, true, true]),\n  any_named: any(list: [false, false, true])\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        sublist_named: [2, 3, 4, 5],
        sublist_len_named: [2, 3, 4],
        append_named: [1, 2, 3, 4],
        insert_named: [1, 99, 2, 3],
        remove_named: [1, 3, 4],
        index_of_named: [2, 4],
        flatten_named: [1, 2, 3, 4, 5],
        count_named: 3,
        min_named: 1,
        max_named: 5,
        sum_named: 15,
        mean_named: 4,
        all_named: true,
        any_named: true,
      });
    });
  });
  describe('fn-product', () => {
    const EXPR = 'product(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [2, 3, 4] }).value)).toEqual(24);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual(6);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(null);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [0, 5] }).value)).toEqual(0);
    });
  });
  describe('fn-remove', () => {
    const EXPR = 'remove(lst, pos)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], pos: 2 }).value)).toEqual([1, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], pos: 1 }).value)).toEqual([2, 3]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], pos: -1 }).value)).toEqual([1, 2]);
    });
  });
  describe('fn-reverse', () => {
    const EXPR = 'reverse(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual([3, 2, 1]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: ['a'] }).value)).toEqual(['a']);
    });
  });
  describe('fn-sort-comparator', () => {
    const EXPR =
      '{\n  sort_numbers_asc: sort([3, 1, 4, 1, 5, 9, 2, 6], function(x, y) x < y),\n  sort_numbers_desc: sort([3, 1, 4, 1, 5, 9, 2, 6], function(x, y) x > y),\n  sort_strings: sort(["banana", "apple", "cherry"], function(x, y) x < y),\n  sort_single: sort([42], function(x, y) x < y),\n  sort_empty: sort([], function(x, y) x < y)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        sort_numbers_asc: [1, 1, 2, 3, 4, 5, 6, 9],
        sort_numbers_desc: [9, 6, 5, 4, 3, 2, 1, 1],
        sort_strings: ['apple', 'banana', 'cherry'],
        sort_single: [42],
        sort_empty: [],
      });
    });
  });
  describe('fn-sort-context', () => {
    const EXPR =
      '{\n  sort_by_name: sort([{name: "Charlie", age: 30}, {name: "Alice", age: 25}, {name: "Bob", age: 35}], function(a, b) a.name < b.name),\n  sort_by_age_desc: sort([{name: "Charlie", age: 30}, {name: "Alice", age: 25}, {name: "Bob", age: 35}], function(a, b) a.age > b.age),\n  sort_numbers_desc: sort([3, 1, 4, 1, 5, 9, 2, 6], function(a, b) a > b),\n  sort_strings: sort(["banana", "apple", "cherry", "date"], function(a, b) a < b),\n  sort_single: sort([42], function(a, b) a < b),\n  sort_empty: sort([], function(a, b) a < b),\n  sort_stable_equal: count(sort([2, 1, 2, 3], function(a, b) a < b))\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        sort_by_name: [
          { name: 'Alice', age: 25 },
          { name: 'Bob', age: 35 },
          { name: 'Charlie', age: 30 },
        ],
        sort_by_age_desc: [
          { name: 'Bob', age: 35 },
          { name: 'Charlie', age: 30 },
          { name: 'Alice', age: 25 },
        ],
        sort_numbers_desc: [9, 6, 5, 4, 3, 2, 1, 1],
        sort_strings: ['apple', 'banana', 'cherry', 'date'],
        sort_single: [42],
        sort_empty: [],
        sort_stable_equal: 4,
      });
    });
  });
  describe('fn-sort', () => {
    const EXPR = 'sort(lst, function(x, y) x < y)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [3, 1, 2] }).value)).toEqual([1, 2, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual([]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [-1, -3, -2] }).value)).toEqual([-3, -2, -1]);
    });
  });
  describe('fn-stddev-mode-edge', () => {
    const EXPR =
      '{\n  stddev_single: stddev([5]),\n  stddev_two: stddev([2, 4]),\n  stddev_uniform: stddev([3, 3, 3]),\n  mode_single: mode([5]),\n  mode_multi: mode([1, 2, 2, 3, 3, 3]),\n  mode_bimodal: mode([1, 1, 2, 2]),\n  duplicate_vals: duplicate values([1, 2, 1, 3, 2, 4]),\n  index_of_multi: index of([1, 2, 3, 2, 1], 2)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        stddev_single: null,
        stddev_two: Math.SQRT2,
        stddev_uniform: 0,
        mode_single: [5],
        mode_multi: [3],
        mode_bimodal: [1, 2],
        duplicate_vals: [1, 2],
        index_of_multi: [2, 4],
      });
    });
  });
  describe('fn-stddev', () => {
    const EXPR = 'stddev(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [2, 4, 4, 4, 5, 5, 7, 9] }).value)).toEqual(2.138089935299395);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1] }).value)).toEqual(null);
    });
  });
  describe('fn-sublist-boundary', () => {
    const EXPR =
      '{\n  from_start: sublist([1,2,3,4,5], 1),\n  with_length: sublist([1,2,3,4,5], 2, 3),\n  negative_pos: sublist([1,2,3,4,5], -3),\n  negative_with_length: sublist([1,2,3,4,5], -3, 2),\n  length_beyond_end: sublist([1,2,3,4,5], 3, 100),\n  last_element: sublist([1,2,3,4,5], 5, 1),\n  single_result: sublist([1,2,3,4,5], 3, 1)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        from_start: [1, 2, 3, 4, 5],
        with_length: [2, 3, 4],
        negative_pos: [3, 4, 5],
        negative_with_length: [3, 4],
        length_beyond_end: [3, 4, 5],
        last_element: [5],
        single_result: [3],
      });
    });
  });
  describe('fn-sublist-length', () => {
    const EXPR = 'sublist(lst, start, len)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3, 4], start: 2, len: 2 }).value)).toEqual([2, 3]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], start: 1, len: 1 }).value)).toEqual([1]);
    });
  });
  describe('fn-sublist', () => {
    const EXPR = 'sublist(lst, start)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3, 4], start: 2 }).value)).toEqual([2, 3, 4]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], start: -2 }).value)).toEqual([2, 3]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3], start: 1 }).value)).toEqual([1, 2, 3]);
    });
  });
  describe('fn-sum', () => {
    const EXPR = 'sum(lst)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { lst: [1, 2, 3] }).value)).toEqual(6);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { lst: [] }).value)).toEqual(null);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { lst: [1.5, 2.5] }).value)).toEqual(4);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { lst: [-1, -2] }).value)).toEqual(-3);
    });
  });
  describe('fn-union', () => {
    const EXPR = 'union(a, b)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: [1, 2, 3], b: [2, 3, 4] }).value)).toEqual([1, 2, 3, 4]);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: [1], b: [1] }).value)).toEqual([1]);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: [], b: [1, 2] }).value)).toEqual([1, 2]);
    });
  });
});
