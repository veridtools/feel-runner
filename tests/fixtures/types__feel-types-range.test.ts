// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { parseDuration } from '../../src/temporal/duration.js';
import { parseDate, parseDateTime, parseTime } from '../../src/temporal/index.js';
import { n } from '../tck/utils.js';

const d = parseDate,
  _t = parseTime,
  _dt = parseDateTime,
  dur = parseDuration;

describe('dmn-fixtures / feel-types / feel-types/range', () => {
  describe('feel-range-null', () => {
    const EXPR =
      '{\n  null_in_closed: null in [1..5],\n  null_in_open: null in (1..5),\n  zero_in_closed: 0 in [1..5],\n  boundary_low: 1 in [1..5],\n  boundary_high: 5 in [1..5],\n  boundary_open_low: 1 in (1..5),\n  boundary_open_high: 5 in (1..5),\n  null_compare_range: null in [null..null],\n  string_range: "b" in ["a".."z"],\n  string_open: "a" in ("a".."z")\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        null_in_closed: null,
        null_in_open: null,
        zero_in_closed: false,
        boundary_low: true,
        boundary_high: true,
        boundary_open_low: false,
        boundary_open_high: false,
        null_compare_range: null,
        string_range: true,
        string_open: false,
      });
    });
  });
  describe('ft-range-comparison', () => {
    const EXPR = 'a in [b..c]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 5, b: 1, c: 10 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 0, b: 1, c: 10 }).value)).toEqual(false);
    });
  });
  describe('ft-range-date', () => {
    const EXPR = 'a in [date("2024-01-01")..date("2024-12-31")]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: d('2024-06-15') }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: d('2023-12-31') }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: d('2024-12-31') }).value)).toEqual(true);
    });
  });
  describe('ft-range-duration', () => {
    const EXPR = 'a in [duration("P1Y")..duration("P5Y")]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P1Y') }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P3Y') }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: dur('P6Y') }).value)).toEqual(false);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { a: dur('P11M') }).value)).toEqual(false);
    });
  });
  describe('ft-range-in-closed', () => {
    const EXPR = 'a in [1..10]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 1 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 10 }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: 5 }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { a: 0 }).value)).toEqual(false);
    });
    it('case 5', () => {
      expect(n(evaluate(EXPR, { a: 11 }).value)).toEqual(false);
    });
  });
  describe('ft-range-in-half-open-left', () => {
    const EXPR = 'a in (1..10]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 1 }).value)).toEqual(false);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 2 }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: 10 }).value)).toEqual(true);
    });
  });
  describe('ft-range-in-half-open-right', () => {
    const EXPR = 'a in [1..10)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 1 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 10 }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: 9 }).value)).toEqual(true);
    });
  });
  describe('ft-range-in-list', () => {
    const EXPR = 'a in [1,3,5,7,9]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 1 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 3 }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: 4 }).value)).toEqual(false);
    });
  });
  describe('ft-range-in-negative', () => {
    const EXPR = 'a in [-10..-1]';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: -5 }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: -10 }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: -1 }).value)).toEqual(true);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { a: 0 }).value)).toEqual(false);
    });
  });
  describe('ft-range-in-open', () => {
    const EXPR = 'a in (1..10)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 1 }).value)).toEqual(false);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 2 }).value)).toEqual(true);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: 10 }).value)).toEqual(false);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { a: 9 }).value)).toEqual(true);
    });
  });
  describe('ft-range-literal-closed', () => {
    const EXPR = '[1..10]';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual('[1..10]');
    });
  });
});
