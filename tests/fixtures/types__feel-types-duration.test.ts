// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { parseDuration } from '../../src/temporal/duration.js';
import { parseDate, parseDateTime, parseTime } from '../../src/temporal/index.js';
import { n } from '../tck/utils.js';

const _d = parseDate,
  _t = parseTime,
  _dt = parseDateTime,
  dur = parseDuration;

describe('dmn-fixtures / feel-types / feel-types/duration', () => {
  describe('feel-duration-arithmetic', () => {
    const EXPR =
      '{\n  ym_sub: duration("P1Y") - duration("P6M"),\n  dt_sub: duration("P1DT2H") - duration("PT30M"),\n  ym_negate: -duration("P1Y2M"),\n  dt_negate: -duration("P1DT2H30M"),\n  dt_mul_number: duration("P1D") * 3,\n  ym_mul_number: duration("P1Y") * 2,\n  dt_div_number: duration("P2DT4H") / 2,\n  ym_div_number: duration("P2Y") / 2,\n  dt_div_duration: duration("PT3H") / duration("PT1H"),\n  ym_div_duration: duration("P2Y") / duration("P6M"),\n  dt_abs_neg: abs(duration("-P1DT2H")),\n  ym_abs_neg: abs(duration("-P1Y6M"))\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        ym_sub: 'P6M',
        dt_sub: 'P1DT1H30M',
        ym_negate: '-P1Y2M',
        dt_negate: '-P1DT2H30M',
        dt_mul_number: 'P3D',
        ym_mul_number: 'P2Y',
        dt_div_number: 'P1DT2H',
        ym_div_number: 'P1Y',
        dt_div_duration: 3,
        ym_div_duration: 4,
        dt_abs_neg: 'P1DT2H',
        ym_abs_neg: 'P1Y6M',
      });
    });
  });
  describe('ft-duration-dt-add', () => {
    const EXPR = 'a + b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P1D'), b: dur('PT12H') }).value)).toEqual('P1DT12H');
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('PT1H'), b: dur('PT30M') }).value)).toEqual('PT1H30M');
    });
  });
  describe('ft-duration-dt-comparison', () => {
    const EXPR = 'a < b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('PT1H'), b: dur('PT2H') }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P1D'), b: dur('PT1H') }).value)).toEqual(false);
    });
  });
  describe('ft-duration-dt-literal', () => {
    const EXPR = 'duration("P2DT3H30M")';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual('P2DT3H30M');
    });
  });
  describe('ft-duration-dt-mul', () => {
    const EXPR = 'a * b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('PT1H'), b: 3 }).value)).toEqual('PT3H');
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P1D'), b: 2 }).value)).toEqual('P2D');
    });
  });
  describe('ft-duration-hours', () => {
    const EXPR = 'hours(a)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('PT3H30M') }).value)).toEqual(3);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P1DT2H') }).value)).toEqual(2);
    });
  });
  describe('ft-duration-years', () => {
    const EXPR = 'years(a)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P2Y6M') }).value)).toEqual(2);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P11M') }).value)).toEqual(0);
    });
  });
  describe('ft-duration-ym-add', () => {
    const EXPR = 'a + b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P1Y'), b: dur('P6M') }).value)).toEqual('P1Y6M');
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P2Y3M'), b: dur('P9M') }).value)).toEqual('P3Y');
    });
  });
  describe('ft-duration-ym-comparison', () => {
    const EXPR = 'a < b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P1Y'), b: dur('P2Y') }).value)).toEqual(true);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P2Y'), b: dur('P1Y') }).value)).toEqual(false);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: dur('P1Y'), b: dur('P1Y') }).value)).toEqual(false);
    });
  });
  describe('ft-duration-ym-literal', () => {
    const EXPR = 'duration("P1Y6M")';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual('P1Y6M');
    });
  });
  describe('ft-duration-ym-mul', () => {
    const EXPR = 'a * b';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: dur('P1Y'), b: 2 }).value)).toEqual('P2Y');
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: dur('P6M'), b: 3 }).value)).toEqual('P1Y6M');
    });
  });
});
