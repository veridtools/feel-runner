// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../src/index.js';
import { n } from '../tck/utils.js';

describe('dmn-fixtures / feel-functions / feel-functions/numeric', () => {
  describe('fn-abs-number', () => {
    const EXPR = 'abs(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 5 }).value)).toEqual(5);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -3 }).value)).toEqual(3);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(0);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: -1.5 }).value)).toEqual(1.5);
    });
  });
  describe('fn-ceiling-scale', () => {
    const EXPR = 'ceiling(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.235, scale: 2 }).value)).toEqual(1.24);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.235, scale: 2 }).value)).toEqual(-1.23);
    });
  });
  describe('fn-ceiling', () => {
    const EXPR = 'ceiling(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.3 }).value)).toEqual(2);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.3 }).value)).toEqual(-1);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 2 }).value)).toEqual(2);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: -0.1 }).value)).toEqual(0);
    });
  });
  describe('fn-decimal-bankers-rounding', () => {
    const EXPR =
      '{\n  half_to_even_0: decimal(2.5, 0),\n  half_to_even_1: decimal(3.5, 0),\n  half_to_even_2: decimal(0.5, 0),\n  half_to_even_3: decimal(1.5, 0),\n  half_to_even_4: decimal(4.5, 0),\n  half_to_even_5: decimal(-0.5, 0),\n  half_to_even_6: decimal(-1.5, 0),\n  scale_2: decimal(3.145, 2),\n  scale_2b: decimal(3.155, 2),\n  no_rounding: decimal(3.1, 2)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        half_to_even_0: 2,
        half_to_even_1: 4,
        half_to_even_2: 0,
        half_to_even_3: 2,
        half_to_even_4: 4,
        half_to_even_5: 0,
        half_to_even_6: -2,
        scale_2: 3.14,
        scale_2b: 3.16,
        no_rounding: 3.1,
      });
    });
  });
  describe('fn-decimal', () => {
    const EXPR = 'decimal(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.115, scale: 2 }).value)).toEqual(1.12);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: 1.125, scale: 2 }).value)).toEqual(1.12);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 1.5, scale: 0 }).value)).toEqual(2);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: -1.5, scale: 0 }).value)).toEqual(-2);
    });
  });
  describe('fn-exp', () => {
    const EXPR = 'exp(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(1);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: 1 }).value)).toEqual(Math.E);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: -1 }).value)).toEqual(Math.exp(-1));
    });
  });
  describe('fn-floor-ceiling-scale', () => {
    const EXPR =
      '{\n  floor_pos: floor(1.9),\n  floor_neg: floor(-1.1),\n  floor_scale1: floor(1.567, 1),\n  floor_neg_scale1: floor(-1.567, 1),\n  ceiling_pos: ceiling(1.1),\n  ceiling_neg: ceiling(-1.9),\n  ceiling_scale1: ceiling(1.123, 1),\n  ceiling_neg_scale1: ceiling(-1.123, 1),\n  floor_zero: floor(3.0),\n  ceiling_integer: ceiling(5)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        floor_pos: 1,
        floor_neg: -2,
        floor_scale1: 1.5,
        floor_neg_scale1: -1.6,
        ceiling_pos: 2,
        ceiling_neg: -1,
        ceiling_scale1: 1.2,
        ceiling_neg_scale1: -1.1,
        floor_zero: 3,
        ceiling_integer: 5,
      });
    });
  });
  describe('fn-floor-scale', () => {
    const EXPR = 'floor(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.235, scale: 2 }).value)).toEqual(1.23);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.235, scale: 2 }).value)).toEqual(-1.24);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 1.5, scale: 0 }).value)).toEqual(1);
    });
  });
  describe('fn-floor', () => {
    const EXPR = 'floor(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.7 }).value)).toEqual(1);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.7 }).value)).toEqual(-2);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 2 }).value)).toEqual(2);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: 0.1 }).value)).toEqual(0);
    });
  });
  describe('fn-log', () => {
    const EXPR = 'log(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1 }).value)).toEqual(0);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: Math.E }).value)).toEqual(1);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(null);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: -1 }).value)).toEqual(null);
    });
  });
  describe('fn-modulo-sign', () => {
    const EXPR =
      '{\n  positive_both: modulo(10, 3),\n  negative_dividend: modulo(-5, 3),\n  negative_divisor: modulo(5, -3),\n  both_negative: modulo(-5, -3),\n  zero_dividend: modulo(0, 7),\n  zero_divisor: modulo(5, 0),\n  exact_div: modulo(9, 3),\n  large: modulo(100, 7)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        positive_both: 1,
        negative_dividend: 1,
        negative_divisor: -1,
        both_negative: -2,
        zero_dividend: 0,
        zero_divisor: null,
        exact_div: 0,
        large: 2,
      });
    });
  });
  describe('fn-modulo', () => {
    const EXPR = 'modulo(a, b)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { a: 10, b: 3 }).value)).toEqual(1);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { a: 10, b: -3 }).value)).toEqual(-2);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { a: -10, b: 3 }).value)).toEqual(2);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { a: 10, b: 5 }).value)).toEqual(0);
    });
  });
  describe('fn-named-params-numeric', () => {
    const EXPR =
      '{\n  decimal_named: decimal(n: 1.115, scale: 2),\n  floor_named: floor(n: -1.1),\n  floor_scale_named: floor(n: 1.567, scale: 1),\n  ceiling_named: ceiling(n: -1.9),\n  ceiling_scale_named: ceiling(n: 1.234, scale: 2),\n  abs_named: abs(n: -42),\n  modulo_named: modulo(dividend: 10, divisor: 3),\n  sqrt_named: sqrt(number: 16),\n  log_named: log(number: 1),\n  exp_named: exp(number: 0),\n  round_up_named: round up(n: 1.234, scale: 2),\n  round_down_named: round down(n: 1.999, scale: 2),\n  round_half_up_named: round half up(n: 1.115, scale: 2),\n  round_half_down_named: round half down(n: 1.115, scale: 2)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        decimal_named: 1.12,
        floor_named: -2,
        floor_scale_named: 1.5,
        ceiling_named: -1,
        ceiling_scale_named: 1.24,
        abs_named: 42,
        modulo_named: 1,
        sqrt_named: 4,
        log_named: 0,
        exp_named: 1,
        round_up_named: 1.24,
        round_down_named: 1.99,
        round_half_up_named: 1.12,
        round_half_down_named: 1.11,
      });
    });
  });
  describe('fn-round-down', () => {
    const EXPR = 'round down(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.235, scale: 2 }).value)).toEqual(1.23);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.235, scale: 2 }).value)).toEqual(-1.23);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 1.9, scale: 0 }).value)).toEqual(1);
    });
  });
  describe('fn-round-half-down', () => {
    const EXPR = 'round half down(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.125, scale: 2 }).value)).toEqual(1.12);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.125, scale: 2 }).value)).toEqual(-1.12);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 2.5, scale: 0 }).value)).toEqual(2);
    });
  });
  describe('fn-round-half-up', () => {
    const EXPR = 'round half up(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.125, scale: 2 }).value)).toEqual(1.13);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.125, scale: 2 }).value)).toEqual(-1.13);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 2.5, scale: 0 }).value)).toEqual(3);
    });
  });
  describe('fn-round-up', () => {
    const EXPR = 'round up(n, scale)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 1.235, scale: 2 }).value)).toEqual(1.24);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: -1.235, scale: 2 }).value)).toEqual(-1.24);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 1.5, scale: 0 }).value)).toEqual(2);
    });
  });
  describe('fn-round-variants', () => {
    const EXPR =
      '{\n  up_pos: round up(1.1),\n  up_neg: round up(-1.1),\n  up_half: round up(1.5),\n  down_pos: round down(1.9),\n  down_neg: round down(-1.9),\n  down_half: round down(1.5),\n  half_up_pos: round half up(1.5),\n  half_up_neg: round half up(-1.5),\n  half_up_half_below: round half up(1.4),\n  half_down_pos: round half down(1.5),\n  half_down_neg: round half down(-1.5)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        up_pos: 2,
        up_neg: -2,
        up_half: 2,
        down_pos: 1,
        down_neg: -1,
        down_half: 1,
        half_up_pos: 2,
        half_up_neg: -2,
        half_up_half_below: 1,
        half_down_pos: 1,
        half_down_neg: -1,
      });
    });
  });
  describe('fn-sqrt-log-boundary', () => {
    const EXPR =
      '{\n  sqrt_zero: sqrt(0),\n  sqrt_one: sqrt(1),\n  sqrt_four: sqrt(4),\n  sqrt_negative: sqrt(-1),\n  log_one: log(1),\n  log_e: log(exp(1)),\n  log_zero: log(0),\n  log_negative: log(-1),\n  exp_zero: exp(0),\n  exp_one: exp(1)\n}';

    it('case 1', () => {
      expect(n(evaluate(EXPR).value)).toEqual({
        sqrt_zero: 0,
        sqrt_one: 1,
        sqrt_four: 2,
        sqrt_negative: null,
        log_one: 0,
        log_e: 1,
        log_zero: null,
        log_negative: null,
        exp_zero: 1,
        exp_one: Math.E,
      });
    });
  });
  describe('fn-sqrt', () => {
    const EXPR = 'sqrt(n)';

    it('case 1', () => {
      expect(n(evaluate(EXPR, { n: 4 }).value)).toEqual(2);
    });
    it('case 2', () => {
      expect(n(evaluate(EXPR, { n: 9 }).value)).toEqual(3);
    });
    it('case 3', () => {
      expect(n(evaluate(EXPR, { n: 2 }).value)).toEqual(Math.SQRT2);
    });
    it('case 4', () => {
      expect(n(evaluate(EXPR, { n: 0 }).value)).toEqual(0);
    });
    it('case 5', () => {
      expect(n(evaluate(EXPR, { n: -1 }).value)).toEqual(null);
    });
  });
});
