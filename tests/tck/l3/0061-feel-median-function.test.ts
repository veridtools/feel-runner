// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision001', 'median([8, 2, 5, 3, 4])'],
  ['decision002', 'median([8, 2, 5, 7])'],
  ['decision003', 'median()'],
  ['decision003_a', 'median(null)'],
  ['decision004', 'median()'],
  ['decision005', 'median([1,2,"foo",4])'],
  ['decision006', 'median([6, 1, 2, 3])'],
  ['decision007', 'median([])'],
  ['decision008', 'median(4)'],
  ['decision009', 'median(8, 2, 5, 3, 4)'],
  ['decision010', 'median(8, 2, 5, 7)'],
  ['decision011', 'median(list:[8, 2, 5, 7])'],
  ['decision012', 'median(list:null)'],
  ['decision013', 'median(l:[2, 4, 7, 5])'],
];

describe('FEEL TCK / 0061-feel-median-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001: will return median for even list size', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001), 4), 'decision001').toBe(true);
  });
  it('002: will return median for odd list size', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision002), 6), 'decision002').toBe(true);
  });
  it('003: will error when no params', () => {
    const ctx = run({});
    expect(n(ctx.decision003)).toBeNull();
  });
  it('003_a: will error when null param', () => {
    const ctx = run({});
    expect(n(ctx.decision003_a)).toBeNull();
  });
  it('004: will error when list item not a number', () => {
    const ctx = run({});
    expect(n(ctx.decision004)).toBeNull();
  });
  it('005: will error when list item not a number', () => {
    const ctx = run({});
    expect(n(ctx.decision005)).toBeNull();
  });
  it('006: will median to decimals', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision006), 2.5), 'decision006').toBe(true);
  });
  it('007: will return null for empty list', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision007), null), 'decision007').toBe(true);
  });
  it('008: will coerce singleton list', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision008), 4), 'decision008').toBe(true);
  });
  it('009: will return median for even varargs list size', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision009), 4), 'decision009').toBe(true);
  });
  it('010: will return median for odd varargs list size', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision010), 6), 'decision010').toBe(true);
  });
  it('011: will return median - named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision011), 6), 'decision011').toBe(true);
  });
  it('012: will error when null named param', () => {
    const ctx = run({});
    expect(n(ctx.decision012)).toBeNull();
  });
  it('013: will error when wrongly named param', () => {
    const ctx = run({});
    expect(n(ctx.decision013)).toBeNull();
  });
});
