// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision001', 'modulo(10, 4)'],
  ['decision001_a', 'modulo(10, -4)'],
  ['decision002', 'modulo(-10, 4)'],
  ['decision003', 'modulo(0, 4)'],
  ['decision003_a', 'modulo(10, 0)'],
  ['decision004', 'modulo()'],
  ['decision005', 'modulo(4)'],
  ['decision005_a', 'modulo(4,4,4)'],
  ['decision006', 'modulo(dividend:10, divisor:4)'],
  ['decision007', 'modulo(dividend:10, foo:4)'],
  ['decision008', 'modulo(null, null)'],
  ['decision008_a', 'modulo(10, null)'],
  ['decision008_b', 'modulo(null, 4)'],
  ['decision009', 'modulo("10", "4")'],
  ['decision010', 'modulo(true, true)'],
  ['decision011', 'modulo(duration("P10D"), 4)'],
  ['decision012', 'modulo(duration("P10Y"), 4)'],
  ['decision013', 'modulo(date("2018-12-06"), 4)'],
  ['decision014', 'modulo(time("10:00:00"), 4)'],
  ['decision015', 'modulo(date and time("2018-12-06T00:00:00"), 4)'],
  ['decision016a', 'modulo(12, 5)'],
  ['decision016b', 'modulo(-12, 5)'],
  ['decision016c', 'modulo(12, -5)'],
  ['decision016d', 'modulo(-12, -5)'],
  ['decision017a', 'modulo(10.1, 4.5)'],
  ['decision017b', 'modulo(-10.1, 4.5)'],
  ['decision017c', 'modulo(10.1, -4.5)'],
  ['decision017d', 'modulo(-10.1, -4.5)'],
];

describe('FEEL TCK / 0056-feel-modulo-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001: Assert on positive numbers', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001), 2), 'decision001').toBe(true);
  });
  it('001_a: Assert on negative divisor', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001_a), -2), 'decision001_a').toBe(true);
  });
  it('002: Assert on negative dividend', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision002), 2), 'decision002').toBe(true);
  });
  it('003: Assert on zero dividend', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision003), 0), 'decision003').toBe(true);
  });
  it('003_a: Assert on zero divisor', () => {
    const ctx = run({});
    expect(n(ctx.decision003_a)).toBeNull();
  });
  it('004: Assert with no params', () => {
    const ctx = run({});
    expect(n(ctx.decision004)).toBeNull();
  });
  it('005: Assert with too few params', () => {
    const ctx = run({});
    expect(n(ctx.decision005)).toBeNull();
  });
  it('005_a: Assert with too many params', () => {
    const ctx = run({});
    expect(n(ctx.decision005_a)).toBeNull();
  });
  it('006: Assert with named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision006), 2), 'decision006').toBe(true);
  });
  it('007: Assert with incorrect named param', () => {
    const ctx = run({});
    expect(n(ctx.decision007)).toBeNull();
  });
  it('008: Assert with null params', () => {
    const ctx = run({});
    expect(n(ctx.decision008)).toBeNull();
  });
  it('008_a: Assert with null params', () => {
    const ctx = run({});
    expect(n(ctx.decision008_a)).toBeNull();
  });
  it('008_b: Assert with null params', () => {
    const ctx = run({});
    expect(n(ctx.decision008_b)).toBeNull();
  });
  it('009: Assert with string param', () => {
    const ctx = run({});
    expect(n(ctx.decision009)).toBeNull();
  });
  it('010: Assert with boolean param', () => {
    const ctx = run({});
    expect(n(ctx.decision010)).toBeNull();
  });
  it('011: Assert with days duration param', () => {
    const ctx = run({});
    expect(n(ctx.decision011)).toBeNull();
  });
  it('012: Assert with months duration param', () => {
    const ctx = run({});
    expect(n(ctx.decision012)).toBeNull();
  });
  it('013: Assert with date param', () => {
    const ctx = run({});
    expect(n(ctx.decision013)).toBeNull();
  });
  it('014: Assert with time param', () => {
    const ctx = run({});
    expect(n(ctx.decision014)).toBeNull();
  });
  it('015: Assert with datetime param', () => {
    const ctx = run({});
    expect(n(ctx.decision015)).toBeNull();
  });
  it('016a: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision016a), 2), 'decision016a').toBe(true);
  });
  it('016b: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision016b), 3), 'decision016b').toBe(true);
  });
  it('016c: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision016c), -3), 'decision016c').toBe(true);
  });
  it('016d: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision016d), -2), 'decision016d').toBe(true);
  });
  it('017a: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision017a), 1.1), 'decision017a').toBe(true);
  });
  it('017b: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision017b), 3.4), 'decision017b').toBe(true);
  });
  it('017c: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision017c), -3.4), 'decision017c').toBe(true);
  });
  it('017d: Example from the DMN Specification, ref DMN13-125', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision017d), -1.1), 'decision017d').toBe(true);
  });
});
