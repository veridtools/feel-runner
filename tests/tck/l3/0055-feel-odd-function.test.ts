// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision001', 'odd(2)'],
  ['decision001_a', 'odd(1)'],
  ['decision002', 'odd(-2)'],
  ['decision002_a', 'odd(-1)'],
  ['decision003', 'odd(0)'],
  ['decision004', 'odd()'],
  ['decision005', 'odd(4,4)'],
  ['decision006', 'odd(number:4)'],
  ['decision007', 'odd(n:4)'],
  ['decision008', 'odd(null)'],
  ['decision009', 'odd("4")'],
  ['decision010', 'odd(true)'],
  ['decision011', 'odd(duration("P4D"))'],
  ['decision012', 'odd(duration("P4Y"))'],
  ['decision013', 'odd(date("2018-12-06"))'],
  ['decision014', 'odd(time("00:00:00"))'],
  ['decision015', 'odd(date and time("2018-12-06T00:00:00"))'],
];

describe('FEEL TCK / 0055-feel-odd-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001: Assert on even positive number', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001), false), 'decision001').toBe(true);
  });
  it('001_a: Assert on positive odd number', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001_a), true), 'decision001_a').toBe(true);
  });
  it('002: Assert on negative even number', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision002), false), 'decision002').toBe(true);
  });
  it('002_a: Assert on negative odd number', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision002_a), true), 'decision002_a').toBe(true);
  });
  it('003: Assert on zero', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision003), false), 'decision003').toBe(true);
  });
  it('004: Assert with no params', () => {
    const ctx = run({});
    expect(n(ctx.decision004)).toBeNull();
  });
  it('005: Assert with too many params', () => {
    const ctx = run({});
    expect(n(ctx.decision005)).toBeNull();
  });
  it('006: Assert with named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision006), false), 'decision006').toBe(true);
  });
  it('007: Assert with incorrect named param', () => {
    const ctx = run({});
    expect(n(ctx.decision007)).toBeNull();
  });
  it('008: Assert with null param', () => {
    const ctx = run({});
    expect(n(ctx.decision008)).toBeNull();
  });
  it('009: Assert with string param', () => {
    const ctx = run({});
    expect(n(ctx.decision009)).toBeNull();
  });
  it('010: Assert with boolean param', () => {
    const ctx = run({});
    expect(n(ctx.decision010)).toBeNull();
  });
  it('011: Assert with days duration param', () => {
    const ctx = run({});
    expect(n(ctx.decision011)).toBeNull();
  });
  it('012: Assert with months duration param', () => {
    const ctx = run({});
    expect(n(ctx.decision012)).toBeNull();
  });
  it('013: Assert with date param', () => {
    const ctx = run({});
    expect(n(ctx.decision013)).toBeNull();
  });
  it('014: Assert with time param', () => {
    const ctx = run({});
    expect(n(ctx.decision014)).toBeNull();
  });
  it('015: Assert with datetime param', () => {
    const ctx = run({});
    expect(n(ctx.decision015)).toBeNull();
  });
});
