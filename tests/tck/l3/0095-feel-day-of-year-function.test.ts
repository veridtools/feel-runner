// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { parseDuration } from '../../../src/temporal/duration.js';
import { parseDate, parseDateTime, parseTime } from '../../../src/temporal/index.js';
import { aeq, type Ctx, n } from '../utils.js';

const d = parseDate,
  _t = parseTime,
  dt = parseDateTime,
  _dur = parseDuration;

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['date_001', 'day of year(@"1970-01-01")'],
  ['date_002', 'day of year(@"2019-12-31")'],
  ['date_003', 'day of year(@"2020-12-31")'],
  ['date_004', 'day of year(@"2019-09-17")'],
  ['date_005', 'day of year(date_input_001)'],
  ['date_006', 'day of year(date: @"2019-09-17")'],
  ['datetime_001', 'day of year(@"1970-01-01T10:10:10")'],
  ['datetime_002', 'day of year(@"2019-12-31T10:10:10")'],
  ['datetime_003', 'day of year(@"2020-12-31T10:10:10")'],
  ['datetime_004', 'day of year(date_input_001)'],
  ['datetime_005', 'day of year(date: @"2019-09-17T10:10:10")'],
  ['null_001', 'day of year(null)'],
  ['null_002', 'day of year()'],
  ['null_003', 'day of year("foo")'],
  ['null_004', 'day of year(date: null)'],
  ['null_005', 'day of year(date: "foo")'],
  ['null_006', 'day of year(value: @"1970-01-01")'],
  ['null_007', 'day of year(date_input_001)'],
  ['null_008', 'day of year(@"1970-01-01", @"1970-01-01")'],
];

describe('FEEL TCK / 0095-feel-day-of-year-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('date_001: date: first day of year', () => {
    const ctx = run({});
    expect(aeq(n(ctx.date_001), 1), 'date_001').toBe(true);
  });
  it('date_002: date: last day of year', () => {
    const ctx = run({});
    expect(aeq(n(ctx.date_002), 365), 'date_002').toBe(true);
  });
  it('date_003: date: leap year has 366 days', () => {
    const ctx = run({});
    expect(aeq(n(ctx.date_003), 366), 'date_003').toBe(true);
  });
  it('date_004: date: spec example', () => {
    const ctx = run({});
    expect(aeq(n(ctx.date_004), 260), 'date_004').toBe(true);
  });
  it('date_005: date: unknown type at compile time with valid date as runtime input', () => {
    const ctx = run({ date_input_001: d('1970-01-01') });
    expect(aeq(n(ctx.date_005), 1), 'date_005').toBe(true);
  });
  it('date_006: date: will accept date as named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.date_006), 260), 'date_006').toBe(true);
  });
  it('datetime_001: datetime: first day of year', () => {
    const ctx = run({});
    expect(aeq(n(ctx.datetime_001), 1), 'datetime_001').toBe(true);
  });
  it('datetime_002: datetime: last day of year', () => {
    const ctx = run({});
    expect(aeq(n(ctx.datetime_002), 365), 'datetime_002').toBe(true);
  });
  it('datetime_003: datetime: leap year has 366 days', () => {
    const ctx = run({});
    expect(aeq(n(ctx.datetime_003), 366), 'datetime_003').toBe(true);
  });
  it('datetime_004: datetime: unknown type at compile time with valid datetime as runtime input', () => {
    const ctx = run({ date_input_001: dt('1970-01-01T10:10:10') });
    expect(aeq(n(ctx.datetime_004), 1), 'datetime_004').toBe(true);
  });
  it('datetime_005: datetime: will accept date time as named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.datetime_005), 260), 'datetime_005').toBe(true);
  });
  it('null_001: null param gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_001)).toBeNull();
  });
  it('null_002: no param gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_002)).toBeNull();
  });
  it('null_003: invalid param type gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_003)).toBeNull();
  });
  it('null_004: null value named param gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_004)).toBeNull();
  });
  it('null_005: invalid type as named param gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_005)).toBeNull();
  });
  it('null_006: invalidly named param gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_006)).toBeNull();
  });
  it('null_007: unknown type at compile time with invalid type as runtime input', () => {
    const ctx = run({ date_input_001: 'foo' });
    expect(n(ctx.null_007)).toBeNull();
  });
  it('null_008: too many params gives null result', () => {
    const ctx = run({});
    expect(n(ctx.null_008)).toBeNull();
  });
});
