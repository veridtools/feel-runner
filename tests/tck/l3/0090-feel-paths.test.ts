// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  [
    'nested_list_001',
    '[{a: {b: 1}}, {a: {b: [2.1, 2.2]}}, {a: {b: 3}}, {a: {b: 4}}, {a: {b: 5}}].a.b =\n                [{b: 1}, {b: [2.1, 2.2]}, {b: 3}, {b: 4}, {b: 5}].b',
  ],
  [
    'nested_list_002',
    '[{b: 1}, {b: [2.1, 2.2]}, {b: 3}, {b: 4}, {b: 5}].b =\n                [1, [2.1, 2.2], 3, 4, 5]',
  ],
  [
    'nested_list_003',
    '[{a: {b: [1]}}, {a: {b: [2.1, 2.2]}}, {a: {b: [3]}}, {a: {b: [4, 5]}}].a.b =\n                [{b: [1]}, {b: [2.1,2.2]}, {b: [3]}, {b: [4, 5]}].b',
  ],
  [
    'nested_list_004',
    '[{b: [1]}, {b: [2.1,2.2]}, {b: [3]}, {b: [4, 5]}].b =\n                [[1], [2.1, 2.2], [3], [4, 5]]',
  ],
];

describe('FEEL TCK / 0090-feel-paths', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it("nested_list_001: Spec 'nested list' example - 1", () => {
    const ctx = run({});
    expect(aeq(n(ctx.nested_list_001), true), 'nested_list_001').toBe(true);
  });
  it("nested_list_002: Spec 'nested list' example - 2", () => {
    const ctx = run({});
    expect(aeq(n(ctx.nested_list_002), true), 'nested_list_002').toBe(true);
  });
  it("nested_list_003: Spec 'nested list' example - DMN13-133", () => {
    const ctx = run({});
    expect(aeq(n(ctx.nested_list_003), true), 'nested_list_003').toBe(true);
  });
  it("nested_list_004: Spec 'nested list' example - DMN13-133", () => {
    const ctx = run({});
    expect(aeq(n(ctx.nested_list_004), true), 'nested_list_004').toBe(true);
  });
});
