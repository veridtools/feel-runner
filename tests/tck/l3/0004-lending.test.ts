// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [['Adjudication', '"ACCEPT"']];

describe('FEEL TCK / 0004-lending', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001', () => {
    const ctx = run({
      ApplicantData: {
        Monthly: { Income: 6000, Expenses: 2000, Repayments: 0 },
        Age: 35,
        ExistingCustomer: true,
        MaritalStatus: 'M',
        EmploymentStatus: 'EMPLOYED',
      },
      RequestedProduct: { ProductType: 'STANDARD LOAN', Amount: 350000, Rate: 0.0395, Term: 360 },
      BureauData: { CreditScore: 649, Bankrupt: false },
      SupportingDocuments: 'YES',
    });
    expect(aeq(n(ctx.Adjudication), 'ACCEPT'), 'Adjudication').toBe(true);
  });
});
