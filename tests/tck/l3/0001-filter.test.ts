// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [['filter01', 'Employees[dept=20].name']];

describe('FEEL TCK / 0001-filter', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001', () => {
    const ctx = run({
      Employees: [
        { id: 7792, dept: 10, name: 'Clark' },
        { id: 7934, dept: 10, name: 'Miller' },
        { id: 7976, dept: 20, name: 'Adams' },
        { id: 7902, dept: 20, name: 'Ford' },
        { id: 7900, dept: 30, name: 'James' },
      ],
    });
    expect(aeq(n(ctx.filter01), ['Adams', 'Ford']), 'filter01').toBe(true);
  });
});
