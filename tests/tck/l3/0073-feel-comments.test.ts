// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision_001', '1 + /* 1 + */ 1'],
  ['decision_002', '1 + // eol comment\n                1'],
  [
    'decision_003',
    '/*\n                some intro waffle\n                */\n                1 + 1 // and stuff',
  ],
];

describe('FEEL TCK / 0073-feel-comments', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('decision_001', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_001), 2), 'decision_001').toBe(true);
  });
  it('decision_002', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_002), 2), 'decision_002').toBe(true);
  });
  it('decision_003', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_003), 2), 'decision_003').toBe(true);
  });
});
