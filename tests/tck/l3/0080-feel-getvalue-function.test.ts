// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision_001', 'get value()'],
  ['decision_002', 'get value({a: "foo"})'],
  ['decision_003', 'get value({a: "foo"}, "a", "bar")'],
  ['decision_004', 'get value({a: "foo"}, "a")'],
  ['decision_005', 'get value("foo", "foo")'],
  ['decision_006', 'get value({a: "foo"}, 123)'],
  ['decision_007', 'get value(key:"a", m:{a: "foo"})'],
  ['decision_008', 'get value(k:"a", m:{a: "foo"})'],
  ['decision_009', 'get value(null, "a")'],
  ['decision_010', 'get value({a: "foo"}, null)'],
  ['decision_011', 'get value(null, null)'],
  ['decision_012', 'get value({a: null}, "a")'],
  ['decision_013', 'get value({a: "foo"}, input_001)'],
];

describe('FEEL TCK / 0080-feel-getvalue-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('decision_001: no param', () => {
    const ctx = run({});
    expect(n(ctx.decision_001)).toBeNull();
  });
  it('decision_002: only one param', () => {
    const ctx = run({});
    expect(n(ctx.decision_002)).toBeNull();
  });
  it('decision_003: too many params', () => {
    const ctx = run({});
    expect(n(ctx.decision_003)).toBeNull();
  });
  it('decision_004: get a value', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_004), 'foo'), 'decision_004').toBe(true);
  });
  it('decision_005: not a map as first param', () => {
    const ctx = run({});
    expect(n(ctx.decision_005)).toBeNull();
  });
  it('decision_006: not a string as second param', () => {
    const ctx = run({});
    expect(n(ctx.decision_006)).toBeNull();
  });
  it('decision_007: named params', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_007), 'foo'), 'decision_007').toBe(true);
  });
  it('decision_008: badly named params', () => {
    const ctx = run({});
    expect(n(ctx.decision_008)).toBeNull();
  });
  it('decision_009: null map param', () => {
    const ctx = run({});
    expect(n(ctx.decision_009)).toBeNull();
  });
  it('decision_010: null key param', () => {
    const ctx = run({});
    expect(n(ctx.decision_010)).toBeNull();
  });
  it('decision_011: null context param', () => {
    const ctx = run({});
    expect(n(ctx.decision_011)).toBeNull();
  });
  it('decision_012: null item value stored in context', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_012), null), 'decision_012').toBe(true);
  });
  it('decision_013: non literal key of string type', () => {
    const ctx = run({ input_001: 'a' });
    expect(aeq(n(ctx.decision_013), 'foo'), 'decision_013').toBe(true);
  });
  it('decision_014: non literal key of incorrect type', () => {
    const ctx = run({ input_001: 12 });
    expect(n(ctx.decision_013)).toBeNull();
  });
});
