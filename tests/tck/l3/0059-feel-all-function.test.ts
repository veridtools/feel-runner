// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision001', 'all([true, false, true])'],
  ['decision002', 'all([true, true, true])'],
  ['decision003', 'all([true, null, true])'],
  ['decision004', 'all([true, 123, true])'],
  ['decision005', 'all([])'],
  ['decision006', 'all(true)'],
  ['decision007', 'all(false)'],
  ['decision008', 'all(null)'],
  ['decision009', 'all(123)'],
  ['decision010', 'all(true, false, true)'],
  ['decision011', 'all(true, true, true)'],
  ['decision012', 'all(true, null, true)'],
  ['decision013', 'all(true, 123, true)'],
  ['decision014', 'all()'],
  ['decision015', 'all(list:[true, false, true])'],
  ['decision016', 'all(list:[true, true, true])'],
  ['decision017', 'all(list:[true, null, true])'],
  ['decision018', 'all(list:null)'],
  ['decision019', 'all(l:[true])'],
];

describe('FEEL TCK / 0059-feel-all-function', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001: return false if any item is false', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision001), false), 'decision001').toBe(true);
  });
  it('002: return true if all items are true', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision002), true), 'decision002').toBe(true);
  });
  it('003: return null if any item is not true or false', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision003), null), 'decision003').toBe(true);
  });
  it('004: return null if any item is not true or false', () => {
    const ctx = run({});
    expect(n(ctx.decision004)).toBeNull();
  });
  it('005: return true for empty list', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision005), true), 'decision005').toBe(true);
  });
  it('006: return true for singleton true ', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision006), true), 'decision006').toBe(true);
  });
  it('007: return false for singleton false', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision007), false), 'decision007').toBe(true);
  });
  it('008: return null for singleton null', () => {
    const ctx = run({});
    expect(n(ctx.decision008)).toBeNull();
  });
  it('009: return null for singleton non-boolean', () => {
    const ctx = run({});
    expect(n(ctx.decision009)).toBeNull();
  });
  it('010: return false if any vararg item is false', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision010), false), 'decision010').toBe(true);
  });
  it('011: return true if all vararg items are true', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision011), true), 'decision011').toBe(true);
  });
  it('012: return null if any vararg item is not true or false', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision012), null), 'decision012').toBe(true);
  });
  it('013: return null if any vararg item is not true or false', () => {
    const ctx = run({});
    expect(n(ctx.decision013)).toBeNull();
  });
  it('014: return error when no params', () => {
    const ctx = run({});
    expect(n(ctx.decision014)).toBeNull();
  });
  it('015: return false if any item is false with named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision015), false), 'decision015').toBe(true);
  });
  it('016: return true if all items are true with named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision016), true), 'decision016').toBe(true);
  });
  it('017: return null if any item is not true or false - with named param', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision017), null), 'decision017').toBe(true);
  });
  it('018: return null when named param is null', () => {
    const ctx = run({});
    expect(n(ctx.decision018)).toBeNull();
  });
  it('019: will error when wrongly named param', () => {
    const ctx = run({});
    expect(n(ctx.decision019)).toBeNull();
  });
});
