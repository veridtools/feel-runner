// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['listGen1', '["a","b","c"]'],
  ['listGen2', '[a,b,c]'],
  ['listGen3', '["a",b,c]'],
  ['listGen6', 'flatten([["w","x"],"y","z"])'],
  ['listGen7', 'flatten([wx,"y","z"])'],
  ['listGen8', 'flatten([a,b,listGen6])'],
  ['listGen9', 'flatten([a,b,listGen7])'],
  ['listGen10', 'flatten([listGen4,listGen7])'],
];

describe('FEEL TCK / 0008-listGen', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001', () => {
    const ctx = run({});
    expect(aeq(n(ctx.listGen1), ['a', 'b', 'c']), 'listGen1').toBe(true);
  });
  it('002', () => {
    const ctx = run({ a: 'a', b: 'b', c: 'c' });
    expect(aeq(n(ctx.listGen2), ['a', 'b', 'c']), 'listGen2').toBe(true);
  });
  it('003', () => {
    const ctx = run({ b: 'b', c: 'c' });
    expect(aeq(n(ctx.listGen3), ['a', 'b', 'c']), 'listGen3').toBe(true);
  });
  it('006', () => {
    const ctx = run({});
    expect(aeq(n(ctx.listGen6), ['w', 'x', 'y', 'z']), 'listGen6').toBe(true);
  });
  it('007', () => {
    const ctx = run({ wx: ['w', 'x'] });
    expect(aeq(n(ctx.listGen7), ['w', 'x', 'y', 'z']), 'listGen7').toBe(true);
  });
  it('008', () => {
    const ctx = run({ a: 'a', b: 'b' });
    expect(aeq(n(ctx.listGen8), ['a', 'b', 'w', 'x', 'y', 'z']), 'listGen8').toBe(true);
  });
  it('009', () => {
    const ctx = run({ a: 'a', b: 'b', wx: ['w', 'x'] });
    expect(aeq(n(ctx.listGen9), ['a', 'b', 'w', 'x', 'y', 'z']), 'listGen9').toBe(true);
  });
});
