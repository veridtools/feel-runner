// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['everyGtTen1', 'every i in priceTable1 satisfies i.price > 10'],
  ['everyGtTen2', 'every i in priceTable2 satisfies i.price > 10'],
  ['someGtTen1', 'some i in priceTable1 satisfies i.price > 10'],
  ['someGtTen2', 'some i in priceTable2 satisfies i.price > 10'],
  ['values in a list', '[0, 1, 2, 3, 4, 5]'],
  ['decision_014', 'every i in values in a list satisfies even(i)'],
  ['decision_014a', 'some i in values in a list satisfies even(i)'],
];

describe('FEEL TCK / 0016-some-every', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001', () => {
    const ctx = run({
      priceTable2: [
        { itemName: 'widget', price: 25 },
        { itemName: 'sprocket', price: 15 },
        { itemName: 'trinket', price: 1.5 },
      ],
    });
    expect(aeq(n(ctx.everyGtTen2), false), 'everyGtTen2').toBe(true);
    expect(aeq(n(ctx.someGtTen2), true), 'someGtTen2').toBe(true);
  });
  it("decision_014: every: 'in' may appear in list identifier", () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_014), false), 'decision_014').toBe(true);
  });
  it("decision_014a: some: 'in' may appear in list identifier", () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_014a), true), 'decision_014a').toBe(true);
  });
});
