// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['decision_001', '3 ** 4 ** 5'],
  ['decision_001_a', '-3 ** 2'],
  ['decision_002', '"foo" ** 4'],
  ['decision_003', 'true ** 4'],
  ['decision_004', 'date("2018-12-10") ** 4'],
  ['decision_005', 'time("10:30:00") ** 4'],
  ['decision_006', 'date and time("2018-12-10") ** 4'],
  ['decision_007', 'duration("P2Y") ** 4'],
  ['decision_008', 'duration("P2D") ** 4'],
  ['decision_009', '{a: 2} ** 4'],
  ['decision_010', '[2] ** 4'],
  ['decision_011', '(function() "foo") ** 4'],
];

describe('FEEL TCK / 0075-feel-exponent', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('decision_001: exponent is LEFT associative!!', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_001), 3486784401), 'decision_001').toBe(true);
  });
  it('decision_001_a: -a**b parses as (-a)**b not -(a**b)', () => {
    const ctx = run({});
    expect(aeq(n(ctx.decision_001_a), 9), 'decision_001_a').toBe(true);
  });
  it('decision_002: string exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_002)).toBeNull();
  });
  it('decision_003: boolean exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_003)).toBeNull();
  });
  it('decision_004: date exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_004)).toBeNull();
  });
  it('decision_005: time exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_005)).toBeNull();
  });
  it('decision_006: date time exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_006)).toBeNull();
  });
  it('decision_007: ym duration exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_007)).toBeNull();
  });
  it('decision_008: dt duration exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_008)).toBeNull();
  });
  it('decision_009: context exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_009)).toBeNull();
  });
  it('decision_010: list exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_010)).toBeNull();
  });
  it('decision_011: function exponent is null', () => {
    const ctx = run({});
    expect(n(ctx.decision_011)).toBeNull();
  });
});
