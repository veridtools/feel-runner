// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [
  ['payment', '(loan.principal*loan.rate/12)/(1-(1+loan.rate/12)**-loan.termMonths)'],
];

describe('FEEL TCK / 0008-LX-arithmetic', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001', () => {
    const ctx = run({ loan: { principal: 600000, rate: 0.0375, termMonths: 360 } });
    expect(aeq(n(ctx.payment), 2778.69354943277), 'payment').toBe(true);
  });
  it('002', () => {
    const ctx = run({ loan: { principal: 30000, rate: 0.0475, termMonths: 60 } });
    expect(aeq(n(ctx.payment), 562.707359373292), 'payment').toBe(true);
  });
  it('003', () => {
    const ctx = run({ loan: { principal: 600000, rate: 0.0399, termMonths: 360 } });
    expect(aeq(n(ctx.payment), 2861.03377700389), 'payment').toBe(true);
  });
});
