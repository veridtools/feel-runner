// Auto-generated by scripts/gen-tck.ts — do not edit manually
import { describe, expect, it } from 'vitest';
import { evaluate } from '../../../src/index.js';
import { aeq, type Ctx, n } from '../utils.js';

// Decisions evaluated in dependency order for every test case
const CHAIN: [string, string][] = [['DecisionNot', 'not(A)']];

describe('FEEL TCK / 0107-feel-ternary-logic-not', () => {
  function run(inputs: Ctx): Ctx {
    const ctx: Ctx = { ...inputs };
    for (const [name, expr] of CHAIN) ctx[name] = evaluate(expr, ctx).value;
    return ctx;
  }

  it('001: Tests ternary logic for true', () => {
    const ctx = run({ A: true });
    expect(aeq(n(ctx.DecisionNot), false), 'DecisionNot').toBe(true);
  });
  it('002: Tests ternary logic for false', () => {
    const ctx = run({ A: false });
    expect(aeq(n(ctx.DecisionNot), true), 'DecisionNot').toBe(true);
  });
  it('003: Tests ternary logic for null', () => {
    const ctx = run({ A: null });
    expect(aeq(n(ctx.DecisionNot), null), 'DecisionNot').toBe(true);
  });
});
